<div>
  {{#if validar}}
    <h1>Listado de productos</h1>
    <ul>
      {{#each bodyInfo}}
        <li>titulo: {{title}}</li>
        <p>Precio: {{price}}</p>
        <p>Stock: {{stock}}</p>
      {{/each}}
    </ul>

  {{else}}
    <h2>{{bodyInfo}}</h2>
  {{/if}}
</div>